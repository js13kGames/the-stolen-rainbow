const {game,assert}=require('./test-support.cjs');const g=game();g.run(`reset();intro=0;p.ci=2;inputKey({key:'!',code:'Digit1',preventDefault(){}},true);cast();assert.equal(p.ci,0,'Shift+1 selects first weapon');keysClear();clearPress();inputKey({key:'ㅋ',code:'KeyZ',preventDefault(){}},true);assert(keys.z,'physical Z works under Korean layout');inputKey({key:'z',code:'KeyZ',preventDefault(){}},false);assert(!keys.z);inputKey({key:'2',code:'Numpad2',preventDefault(){}},true);cast();assert.equal(p.ci,1);`);console.log('PASS shifted number, Korean physical keys, layout-independent keyup and numpad selection');